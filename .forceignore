# Non-Salesforce assets (the web app, docs, agent skills, tooling)
**/node_modules
package.json
package-lock.json
server.js
public/**
docs/**
scripts/**
.agents/**
.claude/**
skills-lock.json
.env
.env.example

# Phase B only — Restriction Rules stay in source control but are NOT deployed
# during Phase A (groundwork/backfill). The current CLI registry fails suffix
# inference for restrictionRule during a --source-dir glob; Phase B removes this
# ignore and deploys them (via explicit manifest if needed) once codes are backfilled.
force-app/main/default/restrictionRules/**

# Salesforce housekeeping
**/*.eslintrc.json
**/jsconfig.json
**/.eslintrc.cjs
